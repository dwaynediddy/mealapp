import React from 'react'
import { View, Text } from 'react-native'
import Breakfast from './components/Breakfast'

//conditional rendering from form results
const generateMeals = () => {
// if btn-1 && || btn-3 === true
    //return <Breakfast /> and lunch and dinner and post workout
    // if combination of buttons === true return lunch and dinner
    //most combintaions to
    //else show lunch and dinner
}

const Plan = () => {
    return (
        <View>
            <Text>meal plan page</Text>
        </View>
    )
}

export default Plan
